# NotebookLM 완벽 학습 & 발표 준비 가이드 (NOTEBOOK_LM_GUIDE.md)

> 본 문서는 구글 **NotebookLM(노트북LM)**을 활용하여 본 프로젝트(`codyssey-b3-02`)의 설계 철학, 코드 구조, 업무 프로세스를 **스스로 완벽히 이해하고, 평가자나 면접관 앞에서 거침없이 설명할 수 있는 수준**으로 만들기 위한 맞춤형 학습 전략서입니다.

---

## 📚 1. NotebookLM에 업로드할 필수 소스 문서 패키지

NotebookLM의 [소스 추가(Add Sources)] 기능에 아래 문서들을 업로드하세요. (마크다운 `.md` 파일을 그대로 드래그 앤 드롭하시면 됩니다.)

| 우선순위 | 파일명 및 경로 | 업로드 이유 (NotebookLM이 학습할 핵심 내용) |
|:---:|---|---|
| **필수 1** | [스터디2.md](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/스터디2.md) | **가장 중요!** 문제 명세서 전체 원문, 실제 소스 코드 스니펫(라인별 주석), 3개월 차 눈높이 비유, 핵심 문법/단어 사전이 모두 포함되어 있어 AI가 코드 세부 동작을 학습하는 핵심 교재입니다. |
| **필수 2** | [PROCESS_ARCHITECTURE.md](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/docs/PROCESS_ARCHITECTURE.md) | 7개 시스템 계층과 파일 ➡️ 함수별 8단계 업무 프로세스 흐름이 체계적으로 정리되어 있어 전체 그림을 잡는 데 필수입니다. |
| **필수 3** | [README.md](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/README.md) | 프로젝트 설치/실행법, CLI 파라미터 제어, 보안 안전 모드, 8가지 평가자 시연 시나리오 및 운영 주의사항을 제공합니다. |
| **필수 4** | [TEST_RESULTS.md](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/TEST_RESULTS.md) | 45개 단위 테스트 스위트의 세부 항목과 검증 결과가 담겨 있어, 예외 처리와 코드 안정성을 어떻게 증명했는지 파악하게 합니다. |
| **선택 5** | [수정할_내용.md](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/수정할_내용.md) | 명세서 100% 대조 감사 결과와 단일 대시(`-model`) 옵션 호환 조치 내역이 기록된 품질 보고서입니다. |

---

## 🎙️ 2. 상황별 NotebookLM 오디오 팟캐스트(Audio Overview) 커스텀 프롬프트

> **💡 사용법**: NotebookLM 오른쪽 상단의 **[Audio Overview (오디오 오버뷰)]** ➡️ **[Customize (맞춤설정)]** 버튼을 누르고 아래 프롬프트 중 원하는 버전을 복사-붙여넣기 하세요.

---

### [버전 1: 올인원 시니어 테크 토크] - 전체 아키텍처와 설계 철학 한 번에 정리
* **목표**: 두 명의 호스트가 이 프로젝트의 핵심 가치와 전체 파이프라인을 유쾌하고 깊이 있게 해설하여 큰 그림을 머릿속에 완성합니다.

```text
너희는 테크 팟캐스트의 베테랑 소프트웨어 엔지니어 진행자 두 명이다.
업로드된 소스 문서를 바탕으로 'Git 커밋 및 PR 자동 생성 CLI 도구(codyssey-b3-02)'에 대한 심층 기술 리뷰(Deep Dive Tech Talk)를 진행해 줘.

[반드시 다뤄야 할 핵심 포인트]
1. 왜 외부 패키지(OpenAI SDK, requests 등)를 전혀 쓰지 않고 파이썬 표준 라이브러리(urllib, subprocess, argparse)만으로 구현했는지에 대한 '경량화 및 무의존성(Zero-dependency)' 설계 철학
2. Git status(--porcelain)와 diff를 안전하게 수집하고, 한글 파일명 깨짐(core.quotepath=false)을 해결한 방법
3. 엔터프라이즈 환경을 고려한 보안 안전 모드(--safe-mode): 정규식 기반 민감정보(API 키, 토큰, 비밀번호, 이메일) 마스킹과 10개 파일/200줄 절단 기술
4. 1회 호출 원칙(Call Count = 1)과 비용 최적화: AI가 형식을 어겼을 때 유료 재호출을 하지 않고 로컬 파이썬(validator.py)으로 0.001초 만에 무료 자동 보정하는 영리한 전략
5. Conventional Commits(50/72자)와 PR 3대 헤더(Why, What, How to Test) 강제 메커니즘

어조는 유쾌하고 활기차며, 청취자가 듣기만 해도 이 시스템의 7개 계층과 동작 원리를 완벽하게 머릿속에 그릴 수 있도록 생생한 비유와 쉬운 설명을 섞어서 대화해 줘.
```

---

### [버전 2: 코딩 3개월 차 입문자 과외 멘토링] - 쉬운 일상 비유와 용어 해설 중심
* **목표**: 어려운 컴퓨터 공학 용어를 몰라도, 일상생활의 비유(자판기, 배달 주문, 매직 칠하기, 선임 개발자의 빨간펜)를 통해 100% 직관적으로 이해합니다.

```text
너희는 비전공자나 코딩 3개월 차 입문자를 가르치는 아주 친절하고 유머러스한 시니어 멘토와 질문 많은 주니어 멘티다.
주니어 멘티가 코드를 보며 궁금해하는 점들을 질문하고, 시니어 멘토가 일상생활의 쉬운 비유를 들어 아주 재미있게 설명해 줘.

[대화에서 꼭 풀어줘야 할 비유들]
- 파이프라인(cli.py): 버튼 하나 누르면 음료가 나오는 '원클릭 자동 자판기'
- Git 연동(git_client.py): 사람 대신 키보드를 치고 영수증을 복사해 오는 '똑똑한 조수'
- 보안 안전 모드(security.py): 주민등록등본을 보낼 때 비밀번호 뒷자리를 검은 매직으로 칠하는 '이중 방패'
- AI REST 통신(ai_client.py): 배달원(urllib)에게 주문서와 지갑(API Key)을 주고 음식을 받아오는 과정
- 검증 및 보정(validator.py): AI 신입사원이 깜빡한 항목을 선임 개발자가 무료로 채워주는 '빨간펜 교정'

멘티가 "왜 requests 라이브러리 안 써요?", "왜 AI한테 다시 써오라고 안 해요?" 같은 질문을 던지면, 멘토가 비용과 안정성 관점에서 무릎을 탁 치게 만드는 명쾌한 설명을 해 줘.
```

---

### [버전 3: 평가관 모의 면접 & 방어 논리 트레이닝] - 압박 면접 100% 합격 대비
* **목표**: 평가자나 기술 면접관이 들어왔을 때 던질 수 있는 날카로운 질문 5가지와, 이에 대한 완벽한 답변 논리를 시뮬레이션합니다.

```text
너희는 까다로운 IT 기술 평가관(면접관)과 이번 프로젝트를 완벽하게 구현한 지원자의 1:1 기술 인터뷰를 시뮬레이션해 줘.
평가관은 날카롭고 집요하게 질문하고, 지원자는 소스 코드에 근거하여 논리정연하고 자신감 있게 방어해 줘.

[필수 인터뷰 질문 5가지]
Q1. "외부 라이브러리를 쓰면 코드 5줄이면 끝날 텐데, 왜 굳이 urllib과 표준 라이브러리만으로 AI 클라이언트를 직접 짜셨나요?"
    -> (방어: 의존성 충돌 제로, 초경량 배포, 환경 제약 없는 실행, HTTP 통신 저수준 메커니즘 이해 증명)
Q2. "AI가 생성한 결과물이 Conventional Commits 글자 수(72자)나 PR 필수 3대 헤더(Why/What/How to Test)를 어기면 어떻게 대응하나요?"
    -> (방어: 비싼 유료 재호출 대신 로컬 정규식 기반 validator.py 후처리기를 통해 1회 호출 원칙 준수 및 서식 100% 강제)
Q3. "개발자가 실수로 diff에 AWS 키나 비밀번호를 넣었을 때 보안 유출 위험은 없나요?"
    -> (방어: apply_safe_mode의 정규식 패턴 마스킹 [REDACTED] 및 10파일/200줄 절단 안전장치)
Q4. "온도(temperature)를 왜 0.2로 낮게 잡으셨나요?"
    -> (방어: 커밋 메시지는 엄격한 규격 준수가 필수적인 공문서이므로 환각과 무작위성을 억제하고 결정론적 응답 유도)
Q5. "테스트는 어떻게 검증하셨나요?"
    -> (방어: unittest.mock을 이용해 외부 OpenAI 서버 과금 없이 401, 429, 네트워크 에러, 후처리 등 45개 케이스 0.03초 만에 검증)

이 대화를 듣는 사람이 면접장에 들어갔을 때 그대로 외워서 대답할 수 있을 정도로 핵심 키워드를 강조해 줘.
```

---

## 🧩 3. 단계별 분할 강의 프롬프트 (NotebookLM 채팅창 질의응답용)

> **💡 사용법**: 한 번에 모든 내용을 공부하기 부담스러울 때, NotebookLM의 **[채팅창(Chat)]**에 아래 프롬프트를 1강부터 4강까지 차례대로 입력하여 단계별 개인 과외를 받으세요.

---

### 📖 [제1강] Git 연동 및 CLI 파이프라인 완전 정복
```text
[제1강 강의 요청: Git 연동과 CLI 아키텍처]
업로드된 '스터디2.md'와 'PROCESS_ARCHITECTURE.md'를 바탕으로, 내가 다른 사람에게 'Git 명령어를 파이썬에 연결하는 방법'을 설명할 수 있도록 강의해 줘.

아래 내용을 꼭 포함해서 아주 알기 쉽게 설명해 줘:
1. `src/cli.py`에서 `argparse` 서브커맨드(`commit`, `pr`)를 어떻게 나누었고, `-model`과 `--model` 같은 단일/더블 대시 옵션을 어떻게 완벽 호환시켰는지
2. `src/git_client.py`의 `run_git_command()`가 `subprocess.run()`을 이용해 어떻게 사람 대신 터미널 출력을 가로채는지(`capture_output=True`)
3. Git에서 한글 파일명이 깨지는 현상을 막기 위해 주입한 `-c core.quotepath=false`의 원리
4. `git status --porcelain`의 앞 두 글자 상태 코드를 잘라내어 파일 목록만 뽑아낸 방법

강의 끝에는 내가 이해했는지 확인할 수 있는 '자가진단 퀴즈 2문제'와 모범 답안도 줘.
```

---

### 📖 [제2강] 엔터프라이즈 보안 안전 모드(--safe-mode) 정복
```text
[제2강 강의 요청: 보안 필터링과 diff 정제]
업로드된 '스터디2.md'의 `src/security.py` 내용을 바탕으로 보안 안전 모드의 메커니즘을 상세히 강의해 줘.

아래 내용을 초보자도 이해할 수 있도록 쉽게 풀어줘:
1. `mask_sensitive_data()` 함수가 정규표현식(Regex)을 이용해 OpenAI 키(`sk-...`), GitHub 토큰(`ghp_...`), AWS 키, 이메일, `password = "..."`를 어떻게 감지하고 `[REDACTED_...]`로 치환하는지
2. `truncate_diff()` 함수가 왜 '최대 10개 파일'과 '최대 200줄'이라는 기준을 두었는지, 그리고 `diff --git ` 헤더를 세어서 11번째 파일부터 잘라내는 원리
3. 실제 기업 실무에서 이 안전 모드가 왜 필수적인 보안 방패가 되는지

설명 후 내가 외워야 할 핵심 키워드 3가지를 정리해 줘.
```

---

### 📖 [제3강] 표준 라이브러리 기반 REST API 연동 및 1회 호출 최적화
```text
[제3강 강의 요청: AI REST 통신과 비용 통제]
업로드된 '스터디2.md'의 `src/ai_client.py`를 바탕으로 AI 연동 및 예외 처리 흐름을 집중 강의해 줘.

아래 핵심 질문들에 답하는 형식으로 설명해 줘:
1. 외부 패키지 `requests`나 `openai` SDK 없이, 파이썬 기본 내장 모듈 `urllib.request`와 `json`만으로 어떻게 HTTP POST 요청 봉투를 만들고 응답을 디코딩하는가?
2. `time.perf_counter()`를 이용한 소요 시간 측정과 `self.call_count`를 통한 1회 호출 원칙 보장 원리
3. HTTP 상태 코드별 예외 분기: 401(인증 실패), 429(한도 초과), 5xx(서버 장애), URLError(인터넷 끊김)를 가로채어 사용자에게 한국어로 친절히 안내하는 메커니즘
4. `temperature=0.2`와 `max_tokens` 파라미터가 AI의 결과물 품질과 과금에 미치는 영향
```

---

### 📖 [제4강] 프롬프트 엔지니어링 및 로컬 무비용 자동 보정(Validator)
```text
[제4강 강의 요청: 프롬프트 설계와 로컬 후처리]
업로드된 '스터디2.md'의 `src/prompts.py`와 `src/validator.py`를 바탕으로 품질 제어 원리를 강의해 줘.

아래 내용을 다뤄줘:
1. 프롬프트에서 `role: system`(페르소나, Conventional Commits, 3대 헤더 규칙)과 `role: user`(파일 목록 요약 + diff)를 분리하여 설계한 이유
2. 커밋 메시지: 1줄 제목 50자(최대 72자) 제한 및 본문 파일/불릿 요약 규칙
3. PR 초안: 1줄 제목(80자) 및 필수 3대 헤더(`Why`, `What`, `How to Test`) 서식
4. **가장 중요한 핵심**: AI가 실수를 했을 때 유료 API를 다시 호출(재생성)하지 않고, 로컬 파이썬 `validator.py`가 정규식으로 감지하여 0.001초 만에 무료로 헤더와 불릿을 주입해 주는 '후처리(Post-processing)'의 기술적 이점
```

---

## 🎯 4. 최종 발표용 3분 스피치 대본 생성 프롬프트

평가자나 면접관 앞에서 **"이 프로젝트에 대해 간단히 소개해 보세요"**라는 요청을 받았을 때 당황하지 않고 3분 동안 완벽하게 브리핑할 수 있는 대본을 생성하는 프롬프트입니다.

```text
업로드된 소스 문서 전체를 바탕으로, 내가 기술 평가관 앞에서 3분 동안 발표할 수 있는 '최종 구두 발표(Presentation) 완벽 스크립트'를 작성해 줘.

[대본 구성]
1. 도입 (30초): 프로젝트의 목표와 핵심 차별점 (무의존성 순수 파이썬, 1회 호출 원칙, 엔터프라이즈 보안 안전 모드)
2. 본론 1 - 데이터 파이프라인 (1분): Git status/diff 수집부터 정규식 마스킹, 프롬프트 조립까지의 흐름
3. 본론 2 - AI 연동 및 비용 최적화 (1분): urllib 경량 REST 통신과 AI 오류를 로컬에서 무료로 고치는 후처리(validator.py) 알고리즘
4. 결론 (30초): 45개 단위 테스트 통과로 입증된 신뢰성과 향후 CI/CD 확장성

말투는 신뢰감 있고 당당한 개발자의 구어체(~했습니다, ~하도록 설계했습니다)로 적어주고, 발표할 때 손짓이나 강조할 단어는 [강조] 표시를 달아줘.
```
