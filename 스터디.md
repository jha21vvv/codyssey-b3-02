# codyssey-b3-02 핵심 코드 분석 및 학습 가이드 (스터디.md)

> 본 문서는 `코디세이 b3-02_문제.txt`의 모든 요구사항이 **코드의 어느 위치에서, 어떤 방식으로 구현되었는지**를 상세히 매핑하고, **코딩 3개월 차 입문자도 직관적으로 이해할 수 있는 쉬운 비유와 해설**을 덧붙인 종합 스터디 가이드입니다.

---

## 📚 목차
1. [Git 변경 사항 수집 모듈 (`src/git_client.py`)](#1-git-변경-사항-수집-모듈-srcgit_clientpy)
2. [보안 안전 모드 & 민감정보 마스킹 모듈 (`src/security.py`)](#2-보안-안전-모드--민감정보-마스킹-모듈-srcsecuritypy)
3. [AI API 연동 및 비용 최적화 모듈 (`src/ai_client.py`)](#3-ai-api-연동-및-비용-최적화-모듈-srcai_clientpy)
4. [프롬프트 엔지니어링 모듈 (`src/prompts.py`)](#4-프롬프트-엔지니어링-모듈-srcpromptspy)
5. [출력 검증 및 자동 보정 모듈 (`src/validator.py`)](#5-출력-검증-및-자동-보정-모듈-srcvalidatorpy)
6. [터미널 구획 분리 포매터 (`src/formatter.py`)](#6-터미널-구획-분리-포매터-srcformatterpy)
7. [CLI 엔트리포인트 및 파이프라인 (`src/cli.py`)](#7-cli-엔트리포인트-및-파이프라인-srcclipy)
8. [자동화 단위 테스트 스위트 (`tests/`)](#8-자동화-단위-테스트-스위트-tests)

---

## 1. Git 변경 사항 수집 모듈 (`src/git_client.py`)

### [요구사항 1] Git 루트 디렉토리 검사
* 📌 **문제 조건**: CLI 실행은 Git이 초기화된 프로젝트 루트 디렉토리에서 수행되어야 한다.
* 📍 **구현 위치**: `src/git_client.py`의 `is_git_repository()` 함수
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  ```python
  def is_git_repository(cwd: Optional[str] = None) -> bool:
      result = run_git_command(["rev-parse", "--is-inside-work-tree"], cwd=cwd)
      return result.returncode == 0 and result.stdout.strip() == "true"
  ```
  파이썬의 `subprocess`를 통해 `git rev-parse --is-inside-work-tree` 명령을 실행합니다. 현재 폴더가 Git 저장소 내부라면 터미널에 `true`가 출력되고 반환 코드(`returncode`)가 0이 되므로, 이를 불리언(`True/False`)으로 판별합니다.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"우리가 식당에 들어갈 때 '여기가 식당 맞나요?' 하고 간판을 확인하는 것과 같아요. Git 명령어를 쓰려면 현재 폴더에 `.git`이라는 보이지 않는 Git 깃발이 꽂혀 있어야 하는데, 이 함수가 바로 '지금 Git 폴더 맞지?' 하고 확인해 주는 문지기 역할을 합니다."*

---

### [요구사항 2] 변경된 파일 목록 및 Diff 내용 수집
* 📌 **문제 조건**: `git status` 결과를 수집해 변경된 파일 목록을 얻고, `git diff` 결과를 수집해 실제 코드 변경 텍스트를 얻어야 한다.
* 📍 **구현 위치**: `src/git_client.py`의 `get_changed_files()` 및 `get_git_diff()` 함수
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  1. `get_changed_files()`: `git status --porcelain` 명령을 실행합니다. `--porcelain` 옵션은 사람이 보는 불필요한 장식 문구를 빼고 `M file.py`, `?? new.txt`처럼 기계가 파싱하기 좋은 일정한 포맷으로 결과를 줍니다. 앞의 상태 코드(2글자)를 잘라내고 파일 경로만 리스트로 반환합니다.
  2. `get_git_diff(staged_only=False)`:
     - `git diff --cached` (장바구니에 담은 staged 변경점)를 먼저 가져옵니다.
     - `staged_only=False`이면 아직 `git add` 안 한 `git diff` 내용까지 함께 합쳐서 반환합니다.
  3. 한글 파일명 깨짐 방지: Git은 기본적으로 한글 파일명을 `\354\275...` 같은 8진수 숫자로 바꿔버리므로, 모든 명령어에 `-c core.quotepath=false` 옵션을 강제 주입하여 한글 파일명도 원본 그대로 읽어오도록 처리했습니다.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"엄마가 마트 갈 때 '무슨 물건을 살지 적어둔 리스트'가 `git status`라면, '각 물건의 구체적인 가격과 개수'가 적힌 상세 영수증이 `git diff`입니다. 이 함수는 우리가 코드를 고친 파일 이름 목록과, 구체적으로 몇 번째 줄이 어떻게 바뀌었는지(+와 -)를 싹 긁어모아 컴퓨터 메모리에 담아두는 역할을 합니다."*

---

### [요구사항 3] 변경 사항 없을 때 안내 후 안전 종료
* 📌 **문제 조건**: 변경 사항이 없을 경우, "변경 사항이 없습니다"에 준하는 메시지를 출력하고 종료해야 한다.
* 📍 **구현 위치**: `src/git_client.py`의 `has_changes()`, `src/cli.py`의 라인 85~88
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  ```python
  if not changed_files and not raw_diff.strip():
      print("[안내] 변경 사항이 없습니다. 코드를 수정하거나 git add 후 다시 실행해주세요.")
      return 0
  ```
  수집된 파일 리스트도 비어있고 diff 텍스트도 공백이면, AI API를 호출하는 비싼 단계로 넘어가지 않고 즉시 안내문을 터미널에 띄운 뒤 `exit code 0`(정상 종료)으로 멈춥니다.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"식당에 손님이 들어왔는데 아무 주문도 안 했는데 주방에 요리를 시키면 돈만 날리겠죠? 고친 코드가 하나도 없는데 비싼 유료 AI를 부르면 토큰 비용만 낭비되니까, 주문표가 비어있으면 '고친 게 없으니 먼저 코드를 고쳐주세요!'라고 친절히 안내하고 끝내는 것입니다."*

---

## 2. 보안 안전 모드 & 민감정보 마스킹 모듈 (`src/security.py`)

### [요구사항 4] diff 내 민감정보 마스킹 및 크기 제한 (`--safe-mode`)
* 📌 **문제 조건**:
  - 보안/민감정보: diff에 포함될 수 있는 민감정보(API Key, 개인정보 등)는 프롬프트에 미포함.
  - `-safe-mode` 같은 옵션으로 제공: (A) diff에서 특정 패턴(API Key, 이메일 등) 마스킹, (B) 최대 10개 파일, 최대 200줄 전송.
* 📍 **구현 위치**: `src/security.py`의 `mask_sensitive_data()`, `truncate_diff()`, `apply_safe_mode()`
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  1. **정규식(Regex) 마스킹**:
     - OpenAI 키: `\bsk-[a-zA-Z0-9]{20,}\b` ➡️ `[REDACTED_API_KEY]`
     - GitHub 토큰: `\b(?:ghp|gho|ghu|ghs|ghr)_[a-zA-Z0-9]{36,}\b` ➡️ `[REDACTED_GITHUB_TOKEN]`
     - AWS 키: `\bAKIA[0-9A-Z]{16}\b` ➡️ `[REDACTED_AWS_KEY]`
     - 이메일: `\b[A-Za-z0-9._%+\-]+@[A-Za-z0-9.\-]+\.[A-Za-z]{2,}\b` ➡️ `[REDACTED_EMAIL]`
     - 패스워드 할당문: `password = "..."` ➡️ `password = "[REDACTED_SECRET]"`
  2. **크기 제한(Truncation)**:
     - `diff --git ` 헤더를 세어 파일이 10개를 넘으면 11번째 파일부터 자르고 `... (외 N개 파일 생략)` 문구 삽입.
     - 전체 라인 수를 세어 200줄이 넘으면 200줄까지만 남기고 `... [diff truncated by safe-mode: 최대 200줄 초과]` 안내 주석을 붙임.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"우리가 주민등록등본을 남에게 보낼 때 주민번호 뒷자리를 매직으로 까맣게 칠해서(마스킹) 보내잖아요? 개발자가 실수로 코드에 적어둔 비밀번호나 API 키가 AI 회사 서버로 넘어가지 않도록 매직으로 칠해주는 것입니다. 또 문서가 1,000장이나 되면 팩스비가 엄청 나오니까 앞부분 200장만 딱 잘라서 보내는 것과 같습니다."*

---

## 3. AI API 연동 및 비용 최적화 모듈 (`src/ai_client.py`)

### [요구사항 5] 환경변수 기반 API Key 관리 및 오류 원인 출력
* 📌 **문제 조건**:
  - API Key는 환경변수로 설정되어야 하며, 코드에 하드코딩하지 않는다.
  - API 호출 실패(네트워크 오류, 인증 실패 등) 시 오류 원인을 포함한 메시지를 출력해야 한다.
* 📍 **구현 위치**: `src/ai_client.py`의 `load_dotenv_file()`, `validate_configuration()`, `generate_completion()`의 예외 처리 블록
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  1. 코드 내부에 `sk-1234...`를 적지 않고, `os.environ.get("OPENAI_API_KEY")`로 운영체제 환경변수에서 키를 읽어옵니다. (루트의 `.env` 파일도 자체 파서로 자동 로드)
  2. 키가 없으면 파이썬 에러 로그(Traceback) 대신 사용자가 바로 터미널에 붙여넣을 수 있는 OS별 설정 명령어(`$env:OPENAI_API_KEY="your_key"`)를 친절히 안내합니다.
  3. HTTP 응답 코드를 분석하여 대응합니다:
     - `HTTP 401`: `AuthenticationError` ➡️ "API 키 인증 실패"
     - `HTTP 429`: `QuotaExceededError` ➡️ "요청 한도 또는 잔여 크레딧 초과"
     - `HTTP 5xx`: `APIError` ➡️ "AI 제공업체 서버 일시 오류"
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"집 열쇠를 현관문 앞에 테이프로 붙여두면 도둑이 들겠죠?(하드코딩 위험) 열쇠는 각자의 주머니(환경변수)에 넣고 다녀야 안전합니다. 그리고 문을 열려고 했는데 열쇠가 안 맞으면 '열쇠가 잘못되었습니다'라고 말을 해줘야지, 문이 갑자기 와르르 무너지면 안 되잖아요? 에러가 났을 때 왜 안 되는지 한국어로 똑똑하게 알려주는 코드입니다."*

---

### [요구사항 6] 비용 최적화 및 1회 호출 제약
* 📌 **문제 조건**: 과도한 비용 방지를 위해 1회 실행 시 요청 횟수 1~2회 이내 권장 (commit/pr 명령 각각 AI API 1회 호출 및 로그에 호출 횟수 출력).
* 📍 **구현 위치**: `src/ai_client.py`의 `self.call_count` 및 `src/formatter.py`의 메타데이터 출력부
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  - `AIClient` 인스턴스가 `generate_completion()`을 실행할 때마다 `self.call_count += 1`로 정확히 카운트합니다.
  - 커밋 메시지 생성 파이프라인에서 AI API는 **단 한 번만 호출**되며, AI 응답에 형식이 조금 어긋나더라도 AI를 재호출(재생성)하지 않고 로컬 파이썬 알고리즘(`validator.py`)으로 보정하여 호출 횟수를 무조건 1회로 유지합니다.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"유료 전화 상담(AI 호출)을 할 때, 말이 조금 안 통한다고 전화를 끊고 다시 걸면 기본요금이 또 나가잖아요? 그래서 전화는 딱 1번만 걸어서 답변을 받아온 뒤, 조금 어색한 부분은 내 컴퓨터가 무료로 알아서 맞춤법과 형식을 고치는(후처리) 영리한 전략입니다."*

---

## 4. 프롬프트 엔지니어링 모듈 (`src/prompts.py`)

### [요구사항 7] 프롬프트 템플릿 설계 (Commit 및 PR 구조 강제)
* 📌 **문제 조건**:
  - 커밋: 변경 사항 요약 기반, 1줄 제목 필수, 본문 1~3개 파일 언급 또는 1~2개 불릿 포함.
  - PR: 1줄 제목(80자 이내), 필수 3대 헤더(`Why`, `What`, `How to Test`) 및 각 섹션 최소 1개 불릿.
* 📍 **구현 위치**: `src/prompts.py`의 `COMMIT_SYSTEM_PROMPT`, `PR_SYSTEM_PROMPT`, `build_commit_prompt()`, `build_pr_prompt()`
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  - **System Role**: AI에게 "숙련된 소프트웨어 엔지니어"라는 페르소나를 부여하고, 불필요한 마크다운 백틱(```)을 감싸지 말 것과 실무 규칙을 지킬 것을 엄격히 명령합니다.
  - **User Role**: 수집된 파일 목록(`format_changed_files_summary()`)과 마스킹 처리된 `diff_text`를 보기 좋은 양식으로 조립하여 AI에 질문으로 전달합니다.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"AI에게 '너 마음대로 써봐'라고 하면 소설을 쓰거나 엉뚱한 대답을 합니다. 그래서 시험지 양식처럼 '1번 칸에는 제목(50자), 2번 칸에는 빈 줄, 3번 칸에는 Why/What/How to Test 헤더를 쓰고 불릿 기호(- )를 반드시 달아줘'라고 꼼꼼한 양식 시험지를 만들어 건네주는 작업입니다."*

---

## 5. 출력 검증 및 자동 보정 모듈 (`src/validator.py`)

### [요구사항 8] 형식 검증 및 로컬 후처리 (Post-processing)
* 📌 **문제 조건**:
  - 커밋 제목: 50자 이내 권장(최대 72자)
  - PR 제목: 최대 80자
  - PR 본문: Why/What/How to Test 헤더 필수 + 각 섹션 최소 1개 불릿
  - 검증 후 재생성 또는 후처리 중 택1
* 📍 **구현 위치**: `src/validator.py`의 `validate_and_fix_commit()`, `validate_and_fix_pr()`, `clean_title()`
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  1. `clean_title(title, max_length)`: 제목이 길면 글자 수를 세어 72자(커밋)나 80자(PR) 지점에서 잘라내고 끝에 `...`을 붙입니다.
  2. `validate_and_fix_commit()`: 첫 줄을 제목으로 잡고, 본문에 불릿이나 파일 언급이 누락되었으면 `changed_files` 정보를 바탕으로 `- 변경된 파일: ...`, `- 핵심 변경 사항 구현 및 반영` 불릿을 자동으로 주입합니다.
  3. `validate_and_fix_pr()`: 본문을 파싱하여 `Why`, `What`, `How to Test` 3개 섹션의 존재 여부를 정규식으로 확인합니다. 만약 AI가 실수로 `How to Test` 섹션을 빠뜨렸다면, 코드가 알아서 `### How to Test\n- 관련 단위 테스트 실행 및 변경 기능 동작 검증` 섹션을 복구해 붙여줍니다. 불릿이 없는 일반 텍스트 라인은 자동으로 `- ` 불릿 기호를 달아줍니다.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"AI라는 신입 사원이 보고서를 써왔는데 제목이 너무 길거나 필수 항목(Why/What/How to Test)을 깜빡 빼먹었을 때, 화내면서 '다시 써와!' 시키는 게 아니라 선임 개발자인 우리 프로그램이 펜을 들고 '제목은 여기서 자르고, 빠진 섹션은 내가 채워둘게' 하고 완벽한 완성품으로 만들어주는 교정기입니다."*

---

## 6. 터미널 구획 분리 포매터 (`src/formatter.py`)

### [요구사항 9] 터미널 구획 분리 및 메타정보 로깅
* 📌 **문제 조건**:
  - 사용자가 결과를 검토할 수 있도록, 최종 출력은 구분선/헤더 등으로 구획이 나뉘어 표시되어야 한다.
  - 생성된 커밋 메시지는 터미널에 출력되어 사용자가 복사하여 적용할 수 있어야 한다.
  - 로그에 호출 횟수를 출력한다.
  - 생성된 문구는 최종 정답이 아니며, 사용자가 검토 후 적용한다.
* 📍 **구현 위치**: `src/formatter.py`의 `format_output()`
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  - `=` 68개로 이루어진 이중 구분선(`DIVIDER_DOUBLE`)과 `-` 68개로 이루어진 단일 구분선(`DIVIDER_SINGLE`)으로 터미널에 예쁜 박스 레이아웃을 그립니다.
  - 상단에는 생성 결과 텍스트만 깔끔하게 두어 마우스로 드래그해 복사하기 쉽게 만듭니다.
  - 하단에는 `[실행 메타정보]` 블록을 두어 `AI API 호출 횟수 : 1회`, `소요 시간`, `사용 모델`, `안전 모드 적용 여부`를 명시하고, 맨 밑에 주의사항(`[안내] 위 결과물은 AI가 생성한 초안입니다...`)을 출력합니다.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"레스토랑에 가면 스테이크가 그냥 접시도 없이 탁자에 나오는 게 아니라, 예쁜 접시에 담기고 밑에 영수증과 '뜨거우니 조심하세요'라는 안내 카드가 같이 나오잖아요? 터미널 글씨들이 마구 뒤섞이지 않게 예쁜 액자(구분선)를 씌워서 개발자가 마우스로 드래그해서 복사하기 편하게 만들어주는 포장지 역할입니다."*

---

## 7. CLI 엔트리포인트 및 파이프라인 (`src/cli.py`)

### [요구사항 10] 서브커맨드 및 CLI 파라미터 제어
* 📌 **문제 조건**:
  - Python 3.10 이상, 순수 터미널 CLI 프로그램 구현.
  - commit 명령과 pr 명령 제공.
  - API 파라미터(모델, temperature, max_tokens) CLI 옵션으로 변경 가능 및 기본값 제공.
  - 단일 실행으로 전체 자동화 흐름 동작.
* 📍 **구현 위치**: `src/cli.py`의 `build_parser()`, `run_commit_pipeline()`, `run_pr_pipeline()`, `main()`
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  - 파이썬 내장 모듈 `argparse`의 `add_subparsers()`를 사용하여 `commit`과 `pr`이라는 명령어 분기를 만듭니다.
  - 공통 옵션(`-m/--model`, `-t/--temperature`, `--max-tokens`, `--staged`, `--safe-mode`)을 파서에 등록합니다.
  - 전체 파이프라인 오케스트레이션:
    `Git 검사` ➡️ `파일/diff 수집` ➡️ `빈 변경점 검사` ➡️ `안전모드 필터링` ➡️ `프롬프트 조립` ➡️ `AI 호출` ➡️ `Validator 교정` ➡️ `Formatter 출력`이 하나의 함수 안에서 물 흐르듯 순차적으로 이어집니다.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"공장의 컨베이어 벨트 총지배인입니다. 사용자가 'commit 만들어줘!'라고 버튼을 누르면, 1번 일꾼(Git 수집) ➡️ 2번 일꾼(보안 마스킹) ➡️ 3번 일꾼(AI 호출) ➡️ 4번 일꾼(검증 교정) ➡️ 5번 일꾼(예쁜 포장) 순서대로 차례차례 일을 시킨 뒤 완제품을 터미널에 딱 내려놓는 사령탑입니다."*

---

## 8. 자동화 단위 테스트 스위트 (`tests/`)

### [요구사항 11] 테스트 자동화 및 검증 무결성
* 📌 **문제 조건**: 과제 목표인 예외 대응, 실무 규칙 만족 검증 등의 동작 원리를 증명.
* 📍 **구현 위치**: `tests/` 폴더 내 7개 테스트 모듈 (총 45개 테스트 케이스)
* ⚙️ **어떻게 만들어졌는가? (기술적 원리)**:
  - `unittest.mock.patch`를 활용하여 외부 네트워크나 Git 저장소 환경을 흉내 냅니다.
  - 실제 OpenAI 서버를 호출하지 않고도 "401 에러가 났을 때 프로그램이 정상적으로 대처하는가?", "제목이 100자일 때 72자로 잘라내는가?"를 컴퓨터가 0.03초 만에 45개 항목을 전자동으로 검사합니다.
* 🐣 **코딩 3개월 차를 위한 쉬운 해설**:
  > *"자동차를 도로에 내보내기 전에 충돌 테스트를 하고 브레이크가 잘 드는지 모의 시험장에서 45가지 검사를 해보는 것과 같아요. 실제로 벽에 쾅 부딪혀보지 않아도 컴퓨터 안에서 시뮬레이션을 돌려 '이 프로그램은 고장 없이 튼튼하다!'라는 합격 도장을 찍어주는 장치입니다."*
