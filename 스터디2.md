# codyssey-b3-02 명세서 원문 기반 종합 학습 가이드 (스터디2.md)

> 본 문서는 `코디세이 b3-02_문제.txt`의 **모든 원문 텍스트를 한 글자도 빠짐없이 포함**하고 있습니다.  
> 각 기능 및 요구사항 단위마다 **[1] 실제 파이썬 소스 코드(라인별 1차 주석 포함)**, **[2] 코딩 3개월 차도 이해할 수 있는 쉬운 동작 원리 및 비유**, **[3] 3개월 차가 모를 만한 핵심 코드/단어/문법 사전**을 완벽하게 정리한 최고 수준의 스터디 문서입니다.

---

## 🎨 시각화 아키텍처 & 심화 학습 가이드 바로가기
* 🧠 **[NEW]** LLM 파라미터 기술 딥다이브: [스터디_온도와_토큰_기술원리.md](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/스터디_온도와_토큰_기술원리.md)
* 전체 프로세스 시각화 가이드 문서: [PROCESS_ARCHITECTURE.md](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/docs/PROCESS_ARCHITECTURE.md)
  - 🖼️ [전체 시스템 아키텍처 이미지](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/docs/diagrams/architecture_overview.png)
  - 🖼️ [커밋 메시지 자동 생성 흐름도](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/docs/diagrams/process_flow_commit.png)
  - 🖼️ [PR 초안 자동 생성 흐름도](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/docs/diagrams/process_flow_pr.png)
  - 🖼️ [보안 마스킹 및 AI 통신 상세 흐름도](file:///c:/Users/안재현/Documents/24_code/2609_codyssey/codyssey-b3-02/docs/diagrams/process_flow_security_and_ai.png)

---

# 1. 미션 소개

```text
AI API 연동이라고 하면 거창하게 들리는데, 사실 핵심은 '어떤 입력을 주냐'에 있습니다. git diff를 넘기고, 의미 있는 커밋 메시지와 PR 설명이 나오게 프롬프트를 설계하는 것이 이번 미션입니다. 단순 API 호출이 아니라, AI 결과물의 품질을 직접 제어하는 방법을 경험합니다.
AI API는 외부 AI 모델의 텍스트 생성 기능을 애플리케이션에서 호출해 사용할 수 있게 해주는 방식입니다. 고객 응대 문장 생성, 문서 요약, 개발 로그 정리처럼 입력값을 받아 AI가 생성 결과를 반환하는 자동화 작업에 활용됩니다.
이번 미션에서는 AI API를 호출하는 Python 코드를 기반으로, Git 변경 사항을 입력값으로 받아 커밋 메시지와 PR 설명을 자동 생성하는 CLI 도구를 개발합니다. 단순 호출에 그치지 않고, API 파라미터와 컨텍스트(Commit/PR 양식, 변경 이유, 요구사항)를 설계해 원하는 품질의 결과가 나오도록 프롬프트를 최적화합니다.
또한 Git 명령 실행 결과를 프로그램에 연결하고, 생성된 결과물을 실제 커밋/PR 작성 흐름에 적용하며 연동·검증·자동화를 하나의 흐름으로 구현합니다. 이를 통해 AI 기능을 서비스에 붙이는 방식을 구현 관점에서 이해하고, 이후 다양한 업무 자동화/개발 보조 도구로 확장할 수 있는 기반을 갖추게 됩니다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/cli.py` (`run_commit_pipeline` 함수, 라인 96~150)
```python
def run_commit_pipeline(args: argparse.Namespace, client: Optional[AIClient] = None) -> int:
    """커밋 메시지 자동 생성 파이프라인을 실행합니다."""
    # 1. Git 저장소 유효성 검증: 현재 폴더가 Git 폴더가 아니면 에러를 내고 종료합니다.
    if not is_git_repository():
        print("[오류] 현재 디렉토리가 Git 저장소가 아닙니다. Git 초기화된 리포지토리에서 실행해주세요.", file=sys.stderr)
        return 1

    # 2. Git 변경 사항 수집: 변경된 파일 목록과 구체적인 코드 diff 텍스트를 읽어옵니다.
    changed_files = get_changed_files()
    raw_diff = get_git_diff(staged_only=args.staged)

    # 변경된 내용이 하나도 없으면 AI를 호출하지 않고 안내 후 정상 종료(0)합니다.
    if not changed_files and not raw_diff.strip():
        print("[안내] 변경 사항이 없습니다. 코드를 수정하거나 git add 후 다시 실행해주세요.")
        return 0

    # 3. 안전 모드(--safe-mode) 필터링: 비밀번호나 API 키가 있으면 [REDACTED]로 가립니다.
    diff_text = apply_safe_mode(raw_diff, enabled=args.safe_mode)

    # 4. 프롬프트 구성: 파일 목록과 diff를 AI에게 건넬 질문지(프롬프트) 형태로 조립합니다.
    messages = build_commit_prompt(changed_files, diff_text)

    # 5. AI API 호출: 준비된 질문지를 AI에게 전송하고 답변과 걸린 시간을 받습니다.
    ai_client = client or AIClient()
    try:
        raw_response, elapsed = ai_client.generate_completion(
            messages=messages,
            model=args.model,
            temperature=args.temperature,
            max_tokens=args.max_tokens
        )
    except ConfigurationError as e:
        print(f"[설정 오류] {str(e)}", file=sys.stderr)
        return 1
    except APIError as e:
        print(f"[API 오류] {str(e)}", file=sys.stderr)
        return 1

    # 6. 형식 검증 및 후처리: AI 응답의 첫 줄을 제목(72자 이내)으로 맞추고 본문을 보정합니다.
    fixed_commit = validate_and_fix_commit(raw_response, changed_files=changed_files)

    # 7. 출력 포맷팅: 터미널에 예쁜 박스와 호출 횟수 등의 메타정보를 붙여 화면에 찍습니다.
    meta = {
        "call_count": ai_client.call_count,
        "elapsed": elapsed,
        "model": args.model,
        "temperature": args.temperature,
        "max_tokens": args.max_tokens,
        "safe_mode": args.safe_mode
    }
    formatted = format_output(title="커밋 메시지 초안", content=fixed_commit, meta=meta)
    print(formatted)
    return 0  # 성공적으로 끝났음을 운영체제에 알림 (종료 코드 0)
```

### 📖 [코딩 3개월 차를 위한 코드 동작 원리 & 쉬운 비유]
> *"이 코드는 공장의 **'컨베이어 벨트 총책임자'**예요.*  
> *사용자가 터미널에서 '커밋 메시지 만들어줘!'라고 버튼을 누르면, 1번 일꾼(Git 검사) ➡️ 2번 일꾼(고친 파일/코드 수집) ➡️ 3번 일꾼(비밀번호 먹칠 가리기) ➡️ 4번 일꾼(AI 질문지 만들기) ➡️ 5번 일꾼(유료 AI에게 전화 걸기) ➡️ 6번 일꾼(맞춤법/글자 수 다듬기) ➡️ 7번 일꾼(예쁜 상자에 담아 출력하기)까지 한 치의 오차도 없이 차례차례 일을 시킨 뒤 완제품을 터미널에 딱 내려놓는 사령탑 역할을 합니다."*

### 💡 [3개월 차가 모를 만한 핵심 코드(단어/문법) 사전]
1. **`pipeline (파이프라인)`**: 석유를 보내는 파이프처럼, 데이터가 한 단계의 출력을 다음 단계의 입력으로 넘겨주며 연속해서 처리되는 작업 흐름을 의미합니다.
2. **`argparse.Namespace`**: 사용자가 터미널에 입력한 옵션들(예: `--model gpt-4o`, `--temperature 0.2`)을 파이썬이 보기 편하게 하나의 객체 주머니에 담아둔 것입니다. `args.model`처럼 점(`.`)을 찍어서 꺼내 씁니다.
3. **`Optional[AIClient] = None`**: `Optional`은 "이 자리에 `AIClient` 객체가 들어올 수도 있고, 아무것도 안 들어올 수도 있다(`None`)"는 뜻의 파이썬 타입 힌트입니다. 주로 테스트할 때 가짜 AI를 집어넣기 위해 사용합니다.
4. **`file=sys.stderr`**: 보통 `print("안녕")`은 일반 화면(표준 출력, stdout)으로 나가지만, `sys.stderr`로 보내면 컴퓨터에게 "이건 일반 안내가 아니라 에러 경고문이야!"라고 명확히 알려주는 표준 에러 통로입니다.
5. **`return 0` 과 `return 1`**: 운영체제(Windows, Linux)와의 약속입니다. 프로그램이 아무 문제 없이 완벽히 끝났으면 `0`을 반환하고, 중간에 에러가 나서 비정상 종료되었으면 `1`을 반환합니다.

---

# 2. 최종 결과물
다음 3가지 결과물을 완성한다.

## 1. AI API 연동 및 자동화 흐름 동작 확인
```text
* 입력/요청: AI API Key를 환경변수로 설정한 뒤, 프로젝트 루트 디렉토리에서 커밋/PR 자동 생성을 수행하는 Python CLI 프로그램을 실행한다.
* 출력/화면: Python이 git status, git diff 결과를 수집해 AI API를 호출하고, 변경 요약·커밋 메시지·PR 초안이 터미널에 출력된다. 사용자는 단일 실행으로 자동화 흐름이 끝까지 동작함을 확인할 수 있다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/ai_client.py` (`load_dotenv_file` 및 `validate_configuration` 함수, 라인 33~86)
```python
def load_dotenv_file(env_path: str = ".env") -> None:
    """루트 경로의 .env 파일이 존재하는 경우 환경변수로 자동 로드합니다."""
    # 컴퓨터에 '.env'라는 파일이 실제로 있는지 확인합니다.
    if os.path.isfile(env_path):
        try:
            # 파일을 글자 깨짐 없는 utf-8 형식으로 열어서 한 줄씩 읽습니다.
            with open(env_path, "r", encoding="utf-8") as f:
                for line in f:
                    line = line.strip()
                    # 빈 줄이 아니고, 주석(#)이 아니며, '=' 기호가 있는 줄만 골라냅니다.
                    if line and not line.startswith("#") and "=" in line:
                        # KEY=VALUE 형태를 딱 1번만 쪼개서 분리합니다.
                        k, v = line.split("=", 1)
                        k = k.strip()
                        v = v.strip().strip("'\"")  # 따옴표가 있으면 벗겨냅니다.
                        # 아직 환경변수에 등록되지 않은 키라면 새로 등록해 줍니다.
                        if k and k not in os.environ:
                            os.environ[k] = v
        except Exception:
            pass  # 파일 읽기 중 에러가 나도 조용히 넘어갑니다.

def validate_configuration(self) -> None:
    """API 키 설정 여부를 검증합니다."""
    # API 키가 비어있거나 None이면 친절한 안내문과 함께 에러를 일으킵니다(raise).
    if not self.api_key:
        raise ConfigurationError(
            "AI API Key가 설정되지 않았습니다.\n"
            "환경변수를 설정해주세요:\n"
            "  - Windows (PowerShell): $env:OPENAI_API_KEY=\"your_key\"\n"
            "  - Windows (CMD): set OPENAI_API_KEY=your_key\n"
            "  - Linux / macOS: export OPENAI_API_KEY=\"your_key\""
        )
```

### 📖 [코딩 3개월 차를 위한 코드 동작 원리 & 쉬운 비유]
> *"비싼 유료 놀이공원에 입장하려면 '자유이용권(API Key)'이 있어야 합니다.*  
> *이 코드는 손님이 주머니(환경변수)나 가방 속 메모지(`.env`)에 자유이용권을 챙겨왔는지 문앞에서 확인합니다. 만약 깜빡하고 안 가져왔다면 화를 내며 쫓아내는 게 아니라, 'Windows 사용자는 `$env:OPENAI_API_KEY="내키"`라고 입력해서 주머니에 넣으세요!'라고 친절한 안내 쪽지를 건네주는 문지기입니다."*

### 💡 [3개월 차가 모를 만한 핵심 코드(단어/문법) 사전]
1. **`os.environ`**: 파이썬이 운영체제(Windows)의 '환경변수 주머니'를 엿볼 수 있는 딕셔너리(`dict`) 형태의 도구입니다. `os.environ["KEY"]`로 값을 꺼내거나 넣을 수 있습니다.
2. **`with open(...) as f:`**: 파일을 열고 작업이 끝나면 자동으로 닫아(`close`)주는 파이썬의 마법 문법입니다. 파일 닫는 것을 깜빡해서 생기는 컴퓨터 메모리 낭비를 완벽히 막아줍니다.
3. **`strip()`**: 문자열 앞뒤에 붙어있는 쓸데없는 공백(띄어쓰기, 엔터 줄바꿈 `\n`)을 가위로 싹둑 잘라내어 깨끗한 글자만 남기는 함수입니다.
4. **`split("=", 1)`**: 문자열을 `=`을 기준으로 자르되, 뒤의 숫자 `1`은 "딱 1번만 잘라라"는 뜻입니다. 값 안에 또 다른 `=`이 들어있어도 안전하게 분리할 수 있습니다.
5. **`raise`**: "여기서 문제가 생겼으니 파이썬아 즉시 경보를 울려라!" 하고 고의로 예외(Exception)를 발생시키는 키워드입니다.

---

## 2. GitHub 리포지토리
```text
* 입력/요청: GitHub에 리포지토리를 생성하고, AI 기반 Git 커밋/PR 자동 생성기 프로젝트(소스 코드 폴더)를 원격 저장소에 push 한다.
* 출력/화면: GitHub 리포지토리에서 소스 코드/파일 구조가 정상적으로 업로드되어 있으며, 커밋 히스토리와 브랜치 작업 흐름을 확인할 수 있다.
```

### 📍 [코드 구현 위치 및 실제 설정 내용 (라인별 주석)]
* **파일 위치**: `.gitignore` (루트 디렉토리)
```gitignore
# 1. 절대 GitHub에 올리면 안 되는 비밀번호/API 키 파일
.env
.env.local

# 2. 파이썬이 실행되면서 임시로 만드는 바이트코드 캐시 폴더
__pycache__/
*.pyc

# 3. 테스트 실행 시 생기는 캐시 폴더
.pytest_cache/

# 4. 가상환경 폴더
venv/
.venv/
```
* **Git 원격 저장소 연결 및 Push 명령어**:
```bash
# 원격 저장소 URL 연결 (origin이라는 이름으로 GitHub 주소를 등록)
git remote add origin https://github.com/jha21vvv/codyssey-b3-02

# 로컬에서 작업한 main 브랜치의 모든 코드와 커밋 히스토리를 GitHub에 업로드(push)
git push -u origin main
```

### 📖 [코딩 3개월 차를 위한 코드 동작 원리 & 쉬운 비유]
> *"내 방 책상 서랍 속에만 보관하던 소스 코드를 인터넷 전시대(GitHub)에 진열하는 작업입니다.*  
> *그런데 진열할 때 내 진짜 집 열쇠나 비밀번호가 적힌 메모지(`.env`)까지 같이 올려버리면 전 세계 해커들이 보고 훔쳐가겠죠? 그래서 `.gitignore`라는 '가림막 커튼'을 쳐서 안전한 소스 코드만 쏙 골라 GitHub에 전시하는 것입니다."*

### 💡 [3개월 차가 모를 만한 핵심 코드(단어/문법) 사전]
1. **`.gitignore`**: Git에게 "이 파일이나 폴더는 절대로 원격 저장소에 올리지 말고 무시(ignore)해!"라고 지정하는 블랙리스트 목록 파일입니다.
2. **`remote (원격)`**: 내 노트북(로컬)이 아니라 인터넷 저 멀리 어딘가에 있는 GitHub 컴퓨터 서버를 부르는 말입니다.
3. **`origin`**: 원격 저장소 주소(`https://github.com/...`)가 너무 기니까, 편하게 부르기 위해 붙여둔 '원격 저장소의 별명(기본값)'입니다.
4. **`push (푸시)`**: 내 컴퓨터에서 차곡차곡 쌓은 커밋들을 인터넷 GitHub 서버로 밀어 올려서 동기화하는 명령어입니다.

---

## 3. 사용 가이드 문서(README.md)
```text
* 입력/요청: 리포지토리의 README.md를 열어 실행 방법을 확인한다.
* 출력/화면: 설치 방법, 환경변수(API Key) 설정 방법, 실행 예시, 커밋/PR 생성 결과 예시, 주의사항(또는 운영 관점)이 포함되어 있어 사용자는 문서만 보고 도구를 실행할 수 있다.
```

### 📍 [코드 구현 위치 및 실제 문서 내용]
* **파일 위치**: `README.md` (루트 디렉토리 발췌)
```markdown
## 4. 시작하기 (Getting Started)

### 환경변수(API Key) 설정
AI 기능을 사용하려면 OpenAI API Key 설정이 필요합니다.

```powershell
# Windows (PowerShell)
$env:OPENAI_API_KEY="sk-your-actual-api-key"
```

## 5. 명령어 사용법 (Usage)

### 1) 커밋 메시지 자동 생성 (`commit`)
```powershell
python -m src.cli commit
```

### 2) Pull Request 초안 자동 생성 (`pr`)
```powershell
python -m src.cli pr
```
```

### 📖 [코딩 3개월 차를 위한 코드 동작 원리 & 쉬운 비유]
> *"이케아 가구나 레고 블록을 사면 박스 안에 들어있는 **'조립 설명서'**입니다.*  
> *이 프로그램을 만든 내가 옆에서 일일이 설명해주지 않아도, 처음 온 평가자나 동료 개발자가 이 설명서만 읽고 명령어를 복사-붙여넣기 하면 1분 만에 완벽하게 도구를 작동시킬 수 있도록 친절하게 적어둔 매뉴얼입니다."*

### 💡 [3개월 차가 모를 만한 핵심 코드(단어/문법) 사전]
1. **`README.md`**: 전 세계 모든 개발 프로젝트의 대문이자 간판입니다. GitHub에 들어가면 첫 화면에 자동으로 이 파일 내용이 렌더링되어 방문자에게 안내를 제공합니다.
2. **`python -m src.cli`**: 파이썬을 실행할 때 파일 경로(`python src/cli.py`) 대신 `-m` 옵션을 쓰면, `src` 폴더를 하나의 정식 모듈(패키지)로 인식하여 폴더 안팎의 다른 파이썬 파일들을 부를 때 경로가 꼬이지 않고 안전하게 실행됩니다.

---

# 3. 과제 목표
이 과제를 마친 후, 학습자는 아래를 스스로 설명할 수 있어야 한다.

## 1. AI API를 REST API 방식으로 연동하는 전체 흐름
```text
1. AI API를 REST API 방식으로 연동하는 과정에서 요청을 구성하고, 응답을 처리하며, 예외를 대응하는 전체 흐름을 말로 설명할 수 있다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/ai_client.py` (`generate_completion` 함수, 라인 101~167)
```python
# 1. 요청(Request) 데이터 구성: 모델명, 대화 내용, 온도, 토큰 제한을 딕셔너리로 만듭니다.
endpoint = f"{self.base_url}/chat/completions"
payload = {
    "model": model,
    "messages": messages,
    "temperature": temperature,
    "max_tokens": max_tokens,
}

# 파이썬 딕셔너리를 인터넷으로 보낼 수 있는 JSON 문자열로 바꾸고 바이트(0과 1)로 인코딩합니다.
req_body = json.dumps(payload).encode("utf-8")
headers = {
    "Content-Type": "application/json",
    "Authorization": f"Bearer {self.api_key}",  # 내 지갑(인증 키)을 봉투 겉면에 붙임
    "User-Agent": "codyssey-b3-02/1.0"
}

# 파이썬 표준 도구인 urllib으로 HTTP POST 편지봉투(요청 객체)를 만듭니다.
req = urllib.request.Request(endpoint, data=req_body, headers=headers, method="POST")

start_time = time.perf_counter()
self.call_count += 1  # 1회 호출 원칙을 지키기 위해 호출 카운터를 1 올립니다.

try:
    # 2. 인터넷을 통해 AI 서버로 전송하고 답변(Response)을 받아옵니다.
    with urllib.request.urlopen(req, timeout=self.timeout) as response:
        elapsed = time.perf_counter() - start_time
        # 받아온 바이트 데이터를 글자로 풀고(decode), JSON으로 읽어 텍스트만 쏙 추출합니다.
        resp_data = json.loads(response.read().decode("utf-8"))
        content = resp_data["choices"][0]["message"]["content"]
        return content.strip(), elapsed

# 3. 예외 대응: 인터넷 전송 중 문제가 생겼을 때의 안전 대처
except urllib.error.HTTPError as e:
    if e.code == 401:
        raise AuthenticationError("AI API 인증에 실패했습니다 (HTTP 401). API 키가 올바른지 확인해주세요.")
    elif e.code == 429:
        raise QuotaExceededError("AI API 요청 한도 또는 크레딧이 초과되었습니다 (HTTP 429).")
    else:
        raise APIError(f"AI API 호출 오류 (HTTP {e.code})")
```

### 📖 [코딩 3개월 차를 위한 코드 동작 원리 & 쉬운 비유]
> *"식당에서 음식을 배달 주문하는 전 과정과 같아요.*  
> 1. *주문서 작성: 먹고 싶은 메뉴와 요구사항을 적고(JSON 요청), 결제할 카드(API Key)를 봉투에 넣습니다.*  
> 2. *배달원 출발: 배달원(urllib)이 식당 주방(OpenAI 서버)으로 주문서를 들고 달려갑니다.*  
> 3. *음식 수령: 요리된 음식(AI 답변)을 받아서 집 식탁 위에 올려놓습니다(JSON 디코딩).*  
> 4. *사고 대처: 만약 카드가 정지되었으면(401 에러), 혹은 식당 재료가 소진되었으면(429 에러) 왜 음식이 못 왔는지 손님에게 한국어로 친절히 알려주는 흐름입니다."*

### 💡 [3개월 차가 모를 만한 핵심 코드(단어/문법) 사전]
1. **`REST API`**: 인터넷 주소(URL)로 컴퓨터끼리 대화하는 표준 방식입니다. 보통 "이 주소로 데이터를 줄 테니 답을 줘" 할 때 `POST` 방식을 씁니다.
2. **`json.dumps()` 와 `json.loads()`**:
   - `dumps` (Dump String): 파이썬 객체(딕셔너리)를 네트워크 전송용 문자열(JSON)로 변환 (포장하기)
   - `loads` (Load String): 네트워크에서 받아온 JSON 문자열을 파이썬 딕셔너리로 변환 (포장 뜯기)
3. **`Authorization: Bearer <KEY>`**: 인터넷 세계의 신분증 규격입니다. "이 토큰을 소지한 자(Bearer)에게 권한을 부여하라"는 국제 표준 헤더입니다.
4. **`time.perf_counter()`**: 파이썬에서 시간을 1억 분의 1초 단위로 정밀하게 재는 초시계입니다. AI 응답에 몇 초가 걸렸는지 잴 때 씁니다.

---

## 2. temperature, max_tokens 파라미터의 영향
```text
2. temperature, max_tokens 등 주요 파라미터가 결과 품질에 어떤 영향을 주는지 구분하고 설명할 수 있다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/cli.py` (`_add_common_options`, 라인 63~74) 및 `src/ai_client.py`
```python
# temperature 옵션 등록: 텍스트 생성의 무작위성(창의성)을 조절합니다.
parser.add_argument(
    "-t", "--temperature", "-temperature",
    type=float,
    default=0.2,  # 기본값을 0.2로 낮게 잡아 엉뚱한 대답을 막고 엄격한 규칙 준수를 유도합니다.
    help="텍스트 생성 다양성 파라미터 (기본값: 0.2)"
)

# max_tokens 옵션 등록: AI가 생성할 수 있는 답변의 최대 글자(토큰) 수를 제한합니다.
parser.add_argument(
    "--max-tokens", "-max-tokens",
    type=int,
    default=default_max_tokens,  # 커밋은 300, PR은 1000으로 기본 제한
    help=f"생성할 최대 토큰 수 (기본값: {default_max_tokens})"
)
```

### 📖 [코딩 3개월 차를 위한 코드 동작 원리 & 쉬운 비유]
> *"1. **`temperature`**는 **'AI의 엉뚱함/상상력 지수'**입니다.*  
> *온도가 1.0처럼 높으면 AI가 상상력이 풍부해져서 시나 농담을 잘하지만, 0.0~0.2처럼 낮추면 가장 확률이 높은 단어만 차분하게 고릅니다. 커밋 메시지는 공문서처럼 규칙이 엄격해야 하므로 온도를 0.2로 낮게 둔 것입니다.*  
> *2. **`max_tokens`**는 **'시험 답안지의 글자 수 제한'**입니다.*  
> *AI는 글자 수가 많아질수록 사용자에게 청구하는 요금이 비싸집니다. 답안지 길이를 300자로 딱 막아두면 AI가 쓸데없이 긴 소설을 쓰지 않아 돈도 아끼고 답변도 빨라집니다."*

### 💡 [3개월 차가 모를 만한 핵심 코드(단어/문법) 사전]
1. **`Token (토큰)`**: AI가 글자를 읽고 세는 기본 단위입니다. 영어는 대략 1개 단어가 1토큰이고, 한글은 글자당 1~2토큰 정도 차지합니다.
2. **`type=float` 과 `type=int`**: 터미널에서 입력한 글자 "0.2"를 파이썬의 실수(소수점 숫자, `float`)로, "300"을 정수(`int`) 숫자로 자동 변환해 주는 옵션입니다.

---

## 3. Git 명령 실행 결과를 프로그램 입력으로 연결하는 방식
```text
3. git status, git diff 등 Git 명령 실행 결과를 프로그램 입력으로 연결하는 방식과 자동화 흐름을 설명할 수 있다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/git_client.py` (`run_git_command` 함수, 라인 10~20)
```python
def run_git_command(args: List[str], cwd: Optional[str] = None) -> subprocess.CompletedProcess:
    """Git 명령을 실행하고 완료된 프로세스 객체를 반환합니다."""
    working_dir = cwd or os.getcwd()  # 작업할 폴더 경로를 가져옵니다.
    
    # 파이썬이 사람 대신 OS 백그라운드에서 git 명령어를 직접 타이핑해 실행합니다.
    return subprocess.run(
        ["git", "-c", "core.quotepath=false"] + args,  # 한글 파일명 깨짐 방지 옵션 강제 주입
        cwd=working_dir,                               # 명령을 실행할 폴더 위치
        capture_output=True,                           # 터미널 화면에 찍히는 출력을 가로챔
        text=True,                                     # 결과를 바이트가 아닌 글자(문자열)로 변환
        encoding="utf-8",                              # 한글이 깨지지 않게 utf-8로 읽기
        errors="replace"                               # 알 수 없는 문자가 있어도 에러 내지 않고 대체
    )
```

### 📖 [코딩 3개월 차를 위한 코드 동작 원리 & 쉬운 비유]
> *"우리가 까만 터미널 창에 `git status`나 `git diff`라고 키보드로 치면 글자들이 화면에 주르륵 뜨잖아요?*  
> *이 코드는 파이썬이 사람의 손가락 역할을 대신해서 컴퓨터에 몰래 Git 명령어를 치고, 화면에 뜬 글자들을 복사(Ctrl+C)해서 파이썬 변수에 쏙 담아두는 방식입니다."*

### 💡 [3개월 차가 모를 만한 핵심 코드(단어/문법) 사전]
1. **`subprocess (서브프로세스)`**: 파이썬 안에서 윈도우의 메모장, 계산기, Git 같은 다른 외부 프로그램을 대신 실행시키고 제어할 수 있는 파이썬 기본 도구입니다.
2. **`capture_output=True`**: Git 명령어가 터미널 화면에 뱉어낸 출력 결과를 가로채서 파이썬 변수(`result.stdout`)에 저장해 두라는 뜻입니다.
3. **`core.quotepath=false`**: Git의 고질적인 문제입니다. Git은 파일 이름에 한글이 들어가면 `\354\275...` 같은 외계어 8진수 숫자로 바꿔버리는데, 이 옵션을 켜면 한글 파일명이 원래대로 예쁘게 읽힙니다.

---

## 4. 프롬프트 구성 원리 (Commit/PR 양식 강제)
```text
4. Commit/Pull Request 양식과 코드 변경 맥락을 포함해 AI가 요구사항에 맞는 요약을 생성하도록 프롬프트를 구성하는 원리를 설명할 수 있다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/prompts.py` (`COMMIT_SYSTEM_PROMPT` 및 `build_commit_prompt`, 라인 10~55)
```python
# 시스템 역할 프롬프트: AI에게 페르소나(역할)와 엄격한 작성 규칙을 부여합니다.
COMMIT_SYSTEM_PROMPT = """너는 숙련된 소프트웨어 엔지니어다. 제공된 Git 변경 사항을 분석하여 직관적이고 표준적인 Git 커밋 메시지를 생성한다.

[작성 규칙]
1. 커밋 제목:
   - 첫 줄에 반드시 1줄로 작성한다.
   - Conventional Commits 접두사(feat, fix, refactor, docs, chore, test 등)를 사용한다.
   - 50자 이내를 권장하며 최대 72자를 넘지 않는다.
2. 커밋 본문:
   - 제목과 본문 사이에는 반드시 빈 줄 1개를 둔다.
   - 변경된 파일 1~3개를 언급하거나 핵심 변경 사항 1~2개를 불릿('- ')으로 요약한다.
3. 마크다운 백틱(```)이나 부가적인 설명 없이 오직 커밋 메시지만 출력한다.
"""

def build_commit_prompt(changed_files: List[str], diff_text: str) -> List[Dict[str, str]]:
    """AI에게 전달할 프롬프트 메시지 리스트를 조립합니다."""
    # 사용자 질문 조립: 어떤 파일이 바뀌었고, diff가 무엇인지 마크다운 구조로 감쌉니다.
    user_content = f"""[수정된 파일 목록]
{format_changed_files_summary(changed_files)}

[Git Diff 변경 내역]
{diff_text}

위 변경 사항을 분석하여 작성 규칙에 맞는 커밋 메시지를 생성하라."""

    # System과 User 역할을 분리하여 AI에 전달할 최종 대화 목록을 완성합니다.
    return [
        {"role": "system", "content": COMMIT_SYSTEM_PROMPT},
        {"role": "user", "content": user_content}
    ]
```

### 📖 [코딩 3개월 차를 위한 코드 동작 원리 & 쉬운 비유]
> *"AI에게 '그냥 요약해줘'라고 하면 두서없이 말을 길게 늘어놓습니다.*  
> *그래서 시험지 윗부분(System)에는 '너는 채점관에게 보고서를 제출하는 10년 차 수석 개발자야. 제목은 1줄로 50자 이내로 쓰고 본문엔 불릿 기호(- )를 꼭 달아줘'라고 엄격한 서식 규칙을 적어두고, 아랫부분(User)에는 '자, 이번에 고친 코드 영수증은 여기 있어'라고 건네주어 완벽한 답안을 쓰게 만드는 원리입니다."*

### 💡 [3개월 차가 모를 만한 핵심 코드(단어/문법) 사전]
1. **`role: "system"` vs `role: "user"`**:
   - `system`: AI의 뇌 구조와 성격, 행동 규칙을 세팅하는 최우선 지침서
   - `user`: 실제 사람이 AI에게 던지는 구체적인 질문 내용
2. **`Conventional Commits`**: 전 세계 개발자들이 커밋을 남길 때 쓰는 국제 표준 규칙입니다 (`feat`: 새로운 기능 추가, `fix`: 버그 수정, `docs`: 문서 수정 등).
3. **`f-string (f"...")`**: 따옴표 앞에 `f`를 붙이면 중괄호 `{변수}` 안에 파이썬 변수나 계산식을 쏙 집어넣어 예쁜 글자로 합쳐주는 아주 편리한 파이썬 문법입니다.

---

## 5. 생성된 커밋/PR 텍스트 검증 및 다듬기 (후처리)
```text
5. 생성된 커밋/PR 텍스트가 실무 규칙(길이·템플릿·표현 제한)을 만족하도록 내용을 검증하고 필요한 부분을 다듬는 이유와 방법을 설명할 수 있다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/validator.py` (`clean_title` 및 `validate_and_fix_commit`, 라인 23~79)
```python
def clean_title(title: str, max_length: int) -> str:
    """제목에서 불필요한 마크다운 기호를 제거하고 최대 길이를 초과할 경우 안전하게 자릅니다."""
    title = title.strip()
    # 혹시 AI가 '# 제목' 처럼 샵 기호를 붙였으면 정규식으로 지워버립니다.
    title = re.sub(r"^#+\s*", "", title)

    # 제목이 72자(커밋)나 80자(PR)를 넘어가면 말줄임표(...)를 붙여 예쁘게 자릅니다.
    if len(title) > max_length:
        title = title[: max_length - 3] + "..."
    return title

def validate_and_fix_commit(raw_text: str, changed_files: Optional[List[str]] = None) -> str:
    """커밋 메시지 형식 검증 및 자동 보정(후처리)"""
    text = strip_markdown_fences(raw_text)  # AI가 감싼 ``` 코드 블록 제거
    lines = text.splitlines()

    title = clean_title(lines[0], max_length=72)  # 첫 줄을 제목으로 잡고 길이 제한
    body_lines = lines[1:]

    # 제목 뒤에 빈 줄이 없으면 정돈하고 본문 텍스트를 만듭니다.
    body_text = "\n".join(body_lines).strip()

    # 본문에 불릿('- ')이나 파일 이름이 있는지 검사합니다.
    has_bullet = any(line.strip().startswith(("- ", "* ")) for line in body_lines)
    files = changed_files or []
    mentions_file = any(f in body_text for f in files) if files else False

    # 만약 AI가 실수로 불릿이나 파일 언급을 빼먹었다면, 파이썬이 스스로 보충해 줍니다!
    if not has_bullet and not mentions_file:
        supplementary = []
        if files:
            supplementary.append(f"- 변경된 파일: {', '.join(files[:3])}")
        supplementary.append("- 핵심 변경 사항 구현 및 반영")
        body_text = (body_text + "\n" if body_text else "") + "\n".join(supplementary)

    return f"{title}\n\n{body_text}"
```

### 📖 [코딩 3개월 차를 위한 코드 동작 원리 & 쉬운 비유]
> *"AI 신입사원이 보고서를 써왔는데 제목이 너무 길거나 필수 항목 하나를 깜빡 빼먹었습니다.*  
> *이때 화를 내면서 '너 다시 써와!(AI 재호출)'를 시키면 유료 전화 통화료(토큰 비용)가 또 나가잖아요? 그래서 똑똑한 선임 개발자인 우리 프로그램이 빨간펜을 들고 '제목 끝은 여기서 자르고, 빠진 항목은 내가 채워둘게' 하고 **무료로 완벽하게 다듬어주는 자동 교정기** 역할을 하는 것입니다."*

### 💡 [3개월 차가 모를 만한 핵심 코드(단어/문법) 사전]
1. **`Post-processing (후처리)`**: AI가 생성한 결과물을 그대로 믿지 않고, 프로그램 로직을 통해 마지막으로 한 번 더 검사하고 다듬는 기법입니다.
2. **`title[:max_length - 3] + "..."`**: 파이썬의 **문자열 슬라이싱** 문법입니다. 문자열의 맨 앞부터 지정한 글자 수까지만 가위로 자르고 뒤에 점 3개를 붙여줍니다.
3. **`any(...)`**: 괄호 안의 여러 조건들 중에서 **"단 하나라도 참(True)이 있는가?"**를 검사하는 파이썬 내장 함수입니다.

---

# 4. 기능 요구 사항

## 4.1. Git 변경 사항 수집

### [세부 요구 1-1] Git 초기화 루트 디렉토리 검사
```text
* CLI 실행은 Git이 초기화된 프로젝트 루트 디렉토리에서 수행되어야 한다.
```
* 📍 **코드 구현 위치**: `src/git_client.py` (`is_git_repository`, 라인 23~26)
```python
def is_git_repository(cwd: Optional[str] = None) -> bool:
    """현재 디렉토리가 Git 저장소 루트 또는 내부에 위치하는지 확인합니다."""
    # git rev-parse 명령어를 찔러봅니다.
    result = run_git_command(["rev-parse", "--is-inside-work-tree"], cwd=cwd)
    # 명령어 실행이 에러 없이 성공(0)했고, 출력 결과가 "true"이면 Git 저장소가 맞습니다!
    return result.returncode == 0 and result.stdout.strip() == "true"
```
* 📖 **3개월 차 해설**: Git 저장소가 아닌 엉뚱한 폴더에서 실행하면 아무런 Git 명령어(status, diff)도 동작하지 않기 때문에, 시작하자마자 문지기처럼 확인하는 코드입니다.
* 💡 **단어 사전**: `rev-parse --is-inside-work-tree`는 Git에게 "지금 작업 공간(Work Tree) 안이 맞니?"라고 물어보는 공식 Git 질의 명령어입니다.

---

### [세부 요구 1-2] git status 수집 및 변경 파일 목록
```text
* CLI 실행 시 git status 결과를 수집하여 변경된 파일 목록을 확인할 수 있어야 한다.
```
* 📍 **코드 구현 위치**: `src/git_client.py` (`get_changed_files`, 라인 37~62)
```python
def get_changed_files(cwd: Optional[str] = None) -> List[str]:
    """git status --porcelain 결과를 파싱하여 변경된 파일 목록을 반환합니다."""
    result = run_git_command(["status", "--porcelain"], cwd=cwd)
    if result.returncode != 0:
        return []

    changed_files = []
    # 한 줄씩 읽으면서 앞의 상태 코드 2글자를 떼어냅니다.
    for line in result.stdout.splitlines():
        line = line.strip()
        if not line:
            continue
        # status 포맷: XY PATH (첫 2자는 상태 코드, 3번째 자리는 공백이므로 [3:]부터가 진짜 파일 경로)
        file_path = line[3:].strip()
        if " -> " in file_path:  # 파일 이름이 바뀌었을 때는 새 이름을 가져옴
            file_path = file_path.split(" -> ")[1].strip()
        changed_files.append(file_path)

    return changed_files
```
* 📖 **3개월 차 해설**: 사람이 보기 위한 장식 문구를 다 빼고, `M main.py`처럼 컴퓨터가 읽기 편한 기계용 포맷으로 파일 이름만 쏙 뽑아내는 작업입니다.
* 💡 **단어 사전**: `--porcelain`은 "도자기"라는 뜻인데, Git 개발진이 "스크립트 프로그램이 쓰기 좋게 깔끔하고 매끈한 형식으로 출력해 달라"는 의미로 붙인 옵션 이름입니다.

---

### [세부 요구 1-3] git diff 수집 및 변경 내용 획득
```text
* CLI 실행 시 git diff 결과를 수집하여 변경 내용(diff 텍스트)을 얻을 수 있어야 한다.
```
* 📍 **코드 구현 위치**: `src/git_client.py` (`get_git_diff`, 라인 64~89)
```python
def get_git_diff(staged_only: bool = False, cwd: Optional[str] = None) -> str:
    """git diff 결과를 수집합니다 (staged와 unstaged를 모두 수집하여 합침)."""
    # 1. git add로 장바구니에 담긴(staged) 변경점을 가져옵니다.
    res_staged = run_git_command(["diff", "--cached"], cwd=cwd)
    staged_diff = res_staged.stdout if res_staged.returncode == 0 else ""

    if staged_only:
        return staged_diff

    # 2. 아직 git add 안 한(unstaged) 일반 변경점도 가져옵니다.
    res_unstaged = run_git_command(["diff"], cwd=cwd)
    unstaged_diff = res_unstaged.stdout if res_unstaged.returncode == 0 else ""

    # 둘 다 존재하면 보기 좋게 합쳐서 하나의 큰 텍스트로 만듭니다.
    diffs = [d.strip() for d in [staged_diff, unstaged_diff] if d.strip()]
    return "\n\n".join(diffs)
```
* 📖 **3개월 차 해설**: `git add`를 한 코드든 아직 안 한 코드든, 개발자가 오늘 수정한 모든 코드의 +/- 내역을 영수증처럼 한꺼번에 모아오는 함수입니다.
* 💡 **단어 사전**: `--cached`는 Git에서 "이미 스테이징 영역(Index)에 담아둔 diff만 보여줘"라는 뜻입니다.

---

### [세부 요구 1-4] 변경 사항 없을 경우 안내 및 정상 종료
```text
* 변경 사항이 없을 경우, “변경 사항이 없습니다”에 준하는 메시지를 출력하고 종료해야 한다.
```
* 📍 **코드 구현 위치**: `src/cli.py` (라인 107~109)
```python
# 변경된 파일 목록도 없고, diff 내용도 공백이면 즉시 멈춥니다.
if not changed_files and not raw_diff.strip():
    print("[안내] 변경 사항이 없습니다. 코드를 수정하거나 git add 후 다시 실행해주세요.")
    return 0  # 정상 종료
```
* 📖 **3개월 차 해설**: 고친 게 아무것도 없는데 유료 AI에게 전화(API 호출)를 걸면 요금만 날리므로, 바로 "고친 내용이 없어요!"라고 알려주고 프로그램을 예쁘게 끝내는 안전장치입니다.

---

## 4.2. AI API 연동

### [세부 요구 2-1] 환경변수 기반 API Key 관리 (하드코딩 금지)
```text
* AI API Key는 환경변수로 설정되어야 하며, 코드에 하드코딩하지 않는다.
```
* 📍 **코드 구현 위치**: `src/ai_client.py` (라인 63~67)
```python
# 코드에 sk-1234... 를 적지 않고, 운영체제 환경변수에서 읽어옵니다.
if api_key is None:
    load_dotenv_file()
    self.api_key = os.environ.get("OPENAI_API_KEY")
else:
    self.api_key = api_key
```
* 📖 **3개월 차 해설**: 비밀번호를 소스 코드에 글자로 적어두면(하드코딩) GitHub에 올렸을 때 전 세계에 유출됩니다. 운영체제 주머니(`os.environ`)에 넣어두고 몰래 꺼내 쓰는 안전한 방식입니다.

---

### [세부 요구 2-2] CLI 실행 시 AI API 호출 및 터미널 출력
```text
* CLI를 실행하면 AI API가 호출되고, 생성된 커밋 메시지 또는 PR 초안 텍스트가 터미널에 출력되어야 한다.
```
* 📍 **코드 구현 위치**: `src/cli.py` (라인 120~149)
```python
# AI 호출
raw_response, elapsed = ai_client.generate_completion(messages=messages, model=args.model)

# 화면 출력 포맷팅 후 터미널 출력
formatted = format_output(title="커밋 메시지 초안", content=fixed_commit, meta=meta)
print(formatted)
```
* 📖 **3개월 차 해설**: 사용자가 명령어를 딱 한 번 치면 AI가 뚝딱 글을 지어서 모니터 화면에 바로 보여주는 핵심 동작입니다.

---

### [세부 요구 2-3] API 호출 실패 시 오류 원인 메시지 출력
```text
* API 호출 실패(네트워크 오류, 인증 실패 등) 시 오류 원인을 포함한 메시지를 출력해야 한다.
```
* 📍 **코드 구현 위치**: `src/ai_client.py` (라인 136~160)
```python
except urllib.error.HTTPError as e:
    if e.code == 401:
        raise AuthenticationError(
            f"AI API 인증에 실패했습니다 (HTTP 401).\n"
            f"API 키가 올바른지 확인해주세요. (원인: {error_msg or 'Invalid API Key'})"
        )
    elif e.code == 429:
        raise QuotaExceededError(
            f"AI API 요청 한도 또는 크레딧이 초과되었습니다 (HTTP 429).\n"
            f"잠시 후 다시 시도하거나 잔여 크레딧을 확인해주세요. (원인: {error_msg or 'Rate limit reached'})"
        )
```
* 📖 **3개월 차 해설**: 키가 틀렸을 때 파이썬이 영어로 외계어 에러(Traceback)를 뿜으면 사용자가 당황하겠죠? 왜 실패했는지(키 오류? 돈 부족? 인터넷 끊김?) 한국어로 명확하게 알려주는 번역기 역할을 합니다.

---

### [세부 요구 2-4] API 호출 파라미터 CLI 옵션 제공 및 기본값
```text
* API 호출 파라미터(예: 모델, temperature, max_tokens)는 CLI 옵션으로 변경 가능해야 한다.
   * 예: -model, -temperature, -max-tokens 같은 옵션을 제공하고 기본값을 둔다.
```
* 📍 **코드 구현 위치**: `src/cli.py` (`_add_common_options`, 라인 55~94)
```python
def _add_common_options(parser: argparse.ArgumentParser, default_max_tokens: int) -> None:
    """서브커맨드 공통 옵션을 추가합니다."""
    # 표준 --model 뿐만 아니라 지문의 예시인 -model 도 완벽 지원하도록 alias 추가
    parser.add_argument(
        "-m", "--model", "-model",
        type=str,
        default="gpt-4o-mini",
        help="사용할 AI 모델명 (기본값: gpt-4o-mini)"
    )
    parser.add_argument(
        "-t", "--temperature", "-temperature",
        type=float,
        default=0.2,
        help="텍스트 생성 다양성 파라미터 (기본값: 0.2)"
    )
    parser.add_argument(
        "--max-tokens", "-max-tokens",
        type=int,
        default=default_max_tokens,
        help=f"생성할 최대 토큰 수 (기본값: {default_max_tokens})"
    )
    # 특정 외부 Git 저장소 경로를 지정할 수 있는 옵션 (-r, --repo, -C)
    parser.add_argument(
        "-r", "--repo", "-C",
        dest="repo",
        type=str,
        default=None,
        help="분석할 대상 Git 저장소 디렉토리 경로 (기본값: 현재 디렉토리)"
    )
```
* 📖 **3개월 차 해설**: 사용자가 아무 옵션도 안 주면 가장 무난한 기본값(`gpt-4o-mini`, `0.2`, `300`, 현재 폴더)으로 동작하고, 특별히 다른 모델이나 온도를 쓰고 싶거나 다른 외부 프로젝트 폴더(`--repo`)를 분석하고 싶을 땐 터미널에서 마음대로 바꿀 수 있게 조절 손잡이를 달아둔 것입니다.

---

## 4.3. 커밋 메시지 자동 생성

### [세부 요구 3-1 ~ 3-5] commit 명령, 요약 기반, 1줄 제목, 파일/불릿 요약 본문
```text
* commit 명령을 실행하면 커밋 메시지가 생성되어 터미널에 출력되어야 한다.
* 커밋 메시지는 변경 사항 요약을 기반으로 생성되어야 한다.
* 출력 결과에는 커밋 제목 1줄이 필수로 포함되어야 한다.
* 커밋 본문은 선택적으로 포함될 수 있다(본문 포함 여부는 구현 선택).
* (요약 품질 최소 기준) 커밋 본문을 포함하는 경우, 본문에 아래 중 1개 이상을 포함해야 한다.
   * 변경된 파일(또는 모듈) 1~3개 언급
   * 핵심 변경 사항 1~2개를 불릿으로 요약
```
* 📍 **코드 구현 위치**: `src/validator.py` (`validate_and_fix_commit`, 라인 34~78)
```python
# 1줄 제목을 최대 72자 이내로 정돈
title = clean_title(lines[0], max_length=72)
body_lines = lines[1:]

# 본문에 파일명이나 불릿('- ')이 포함되어 있는지 품질 검증
has_bullet = any(line.strip().startswith(("- ", "* ")) for line in body_lines)
files = changed_files or []
mentions_file = any(f in body_text for f in files) if files else False

# 요약 품질 최소 기준이 누락되었으면 파이썬이 자동으로 보충!
if not has_bullet and not mentions_file:
    supplementary = []
    if files:
        target_files = ", ".join(files[:3])
        supplementary.append(f"- 변경된 파일: {target_files}")
    supplementary.append("- 핵심 변경 사항 구현 및 반영")
    body_text = (body_text + "\n" if body_text else "") + "\n".join(supplementary)
```
* 📖 **3개월 차 해설**: 제목 1줄과 본문의 필수 요건(고친 파일 이름 적기, 불릿 기호로 요약하기)을 컴퓨터가 일일이 확인하고, 만약 빠져 있으면 선임 개발자가 체크리스트를 채워 넣듯 완벽하게 완성해 줍니다.

---

### [세부 요구 3-6] 터미널 출력 및 복사 편의성
```text
* 생성된 커밋 메시지는 터미널에 출력되어 사용자가 복사하여 적용할 수 있어야 한다.
```
* 📍 **코드 구현 위치**: `src/formatter.py` (라인 28~32)
```python
DIVIDER_DOUBLE = "=" * 68
DIVIDER_SINGLE = "-" * 68

lines = [
    DIVIDER_DOUBLE,
    f"  [생성 결과: {title}]",
    DIVIDER_SINGLE,
    content.strip(),  # 마우스로 드래그해서 복사하기 아주 좋은 순수 텍스트 영역
    DIVIDER_SINGLE,
]
```
* 📖 **3개월 차 해설**: 지저분한 로그들과 섞이지 않게 위아래로 깔끔한 줄표(`-`, `=`)를 그어주어, 개발자가 마우스로 드래그해서 `git commit -m "..."`에 바로 붙여넣을 수 있게 만든 배려입니다.

---

## 4.4. Pull Request 제목/본문 자동 생성

### [세부 요구 4-1 ~ 4-4] PR 생성 명령, 1줄 제목(80자), Why/What/How to Test 3대 헤더 및 불릿
```text
* CLI 실행 시, PR 제목/본문 초안을 생성하는 명령어를 실행할 수 있어야 한다.
* PR 본문은 템플릿 구조를 가져야 하며 아래 섹션 헤더를 포함해야 한다.
   * Why(변경 배경)
   * What(핵심 변경 사항)
   * How to Test(테스트 방법)
* 각 섹션에는 최소 1개 이상의 불릿이 포함되어야 한다.
* PR 제목은 1줄로 출력되어야 하며, PR 본문과 함께 터미널에서 확인할 수 있어야 한다.
```
* 📍 **코드 구현 위치**: `src/validator.py` (`validate_and_fix_pr`, 라인 81~158)
```python
def validate_and_fix_pr(raw_text: str, changed_files: Optional[List[str]] = None) -> str:
    # 1. 제목을 1줄, 최대 80자 이내로 다듬음
    title = clean_title(lines[0], max_length=80)
    
    # 2. 본문에서 Why, What, How to Test 헤더가 있는지 정규식으로 검사
    sections = {"Why": False, "What": False, "How to Test": False}
    for sec in sections:
        if re.search(rf"###\s*{re.escape(sec)}", body_text, re.IGNORECASE):
            sections[sec] = True

    # 3. 만약 누락된 헤더가 있다면 자동으로 표준 템플릿과 불릿을 생성하여 붙여넣음!
    fallback_templates = {
        "Why": "### Why\n- 코드 품질 향상 및 요구사항 명세 반영",
        "What": "### What\n- 주요 비즈니스 로직 및 모듈 구현/수정",
        "How to Test": "### How to Test\n- 관련 단위 테스트 실행 및 변경 기능 동작 검증"
    }
    for sec, exists in sections.items():
        if not exists:
            body_text += f"\n\n{fallback_templates[sec]}"
            
    return f"{title}\n\n{body_text}"
```
* 📖 **3개월 차 해설**: GitHub에 PR을 올릴 때 회사 팀장님이 "Why, What, How to Test 칸 안 채우면 승인 안 해준다!"라고 규칙을 정해둔 것과 같아요. AI가 깜빡하더라도 프로그램이 정규식으로 감지해서 3대 칸을 무조건 채워줍니다.
* 💡 **단어 사전**: `re.search(r"###\s*Why", ...)`는 파이썬의 **정규표현식(Regex)** 도구로, 본문 중에 `### Why`라는 글자가 있는지 형태를 분석해 찾아내는 탐정 함수입니다.

---

## 4.5. 출력 형식 검증 및 다듬기

### [세부 요구 5-1] 커밋 50자(최대 72자), PR 80자 길이 검증 및 후처리
```text
* 생성된 커밋/PR 텍스트는 아래 길이/형식 규칙을 적용할 수 있어야 한다(검증 후 재생성 또는 후처리 중 택1).
   * 커밋 제목: 50자 이내 권장(최대 72자)
   * PR 제목: 최대 80자
   * PR 본문: Why/What/How to Test 섹션 헤더 필수 + 각 섹션 최소 1개 불릿
```
* 📍 **코드 구현 위치**: `src/validator.py` (`clean_title` 함수, 라인 23~32)
```python
if len(title) > max_length:
    # 최대 길이를 넘어가면 끝을 자르고 말줄임표(...)를 붙입니다.
    title = title[: max_length - 3] + "..."
```
* 📖 **3개월 차 해설**: 글자 수가 넘어가면 말줄임표로 딱 예쁘게 잘라주는 자동 재단기입니다. 돈 드는 '재호출' 대신 0.0001초 만에 끝나는 파이썬 로컬 '후처리' 방식을 택해 비용과 속도를 모두 잡았습니다.

---

### [세부 요구 5-2] 구분선/헤더 구획 분리
```text
* 사용자가 결과를 검토할 수 있도록, 최종 출력은 구분선/헤더 등으로 구획이 나뉘어 표시되어야 한다.
```
* 📍 **코드 구현 위치**: `src/formatter.py` (`format_output`, 라인 27~43)
```python
lines = [
    DIVIDER_DOUBLE,
    f"  [생성 결과: {title}]",
    DIVIDER_SINGLE,
    content.strip(),
    DIVIDER_SINGLE,
    "  [실행 메타정보]",
    f"  * AI API 호출 횟수 : {call_count}회",
    f"  * 소요 시간        : {elapsed:.2f}초",
    f"  * 사용 모델        : {model} (temp={temperature}, max_tokens={max_tokens})",
    f"  * 보안 안전 모드   : {'적용됨 (ON)' if safe_mode else '미적용 (OFF)'}",
    DIVIDER_DOUBLE,
    "  [안내] 위 결과물은 AI가 생성한 초안입니다. 반드시 검토 후 적용하세요.",
    DIVIDER_DOUBLE,
]
```
* 📖 **3개월 차 해설**: 터미널 화면에 결과물이 액자처럼 예쁜 상자에 담겨 나오고, 아래쪽에 몇 초 걸렸는지, 호출은 몇 번 했는지 투명하게 보여주는 영수증입니다.

---

## 4.6. 리포지토리 및 문서화

### [세부 요구 6-1] GitHub 리포지토리 push
```text
* 결과물은 GitHub 리포지토리에 push 되어 있어야 한다.
```
* 📍 **코드 구현 위치**: `https://github.com/jha21vvv/codyssey-b3-02`
* 📖 **3개월 차 해설**: 완성된 코드를 내 컴퓨터에만 두지 않고, `git push`로 원격 저장소에 업로드하여 채점관이 언제든 볼 수 있게 제출하는 요건입니다.

---

### [세부 요구 6-2] README.md 필수 수록 항목
```text
* README.md에는 아래 내용이 포함되어야 한다.
   * 설치 및 실행 방법
   * 환경변수(API Key) 설정 방법
   * 커밋/PR 생성 명령 사용 예시
   * 출력 예시(커밋 메시지 / PR 본문 예시)
   * 아래 항목 중 1개 이상 포함
      * 민감정보 포함 가능성과 대응(마스킹/제외/안전 모드 안내)
      * 비용/요청 횟수 제한 및 권장 사용법
```
* 📍 **코드 구현 위치**: `README.md` (섹션 4, 5, 6, 7, 8 전반)
* 📖 **3개월 차 해설**: 문제에서 요구한 '민감정보 대응'과 '비용/요청 제한' 둘 중 1개만 넣어도 되지만, 우리 프로젝트는 **둘 다 완벽하게 작성**하여 완벽한 점수를 받도록 구성했습니다.

---

# 5. 개발 환경
```text
* Python 3.10 이상
* 웹 화면 없이, 터미널에서 실행되는 프로그램으로 구현한다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `pyproject.toml` 및 소스 전반
```python
# 파이썬 3.10 이상의 현대적인 타입 힌팅 문법 적극 사용
from typing import List, Optional, Tuple, Dict, Any

# 외부 웹 프레임워크(Django, React) 없이 순수 터미널 표준 입출력만 사용
import sys
print("터미널 화면에 출력됩니다")
```
* 📖 **3개월 차 해설**: 무거운 웹사이트나 마우스 클릭 프로그램이 아니라, 검은 터미널 창에서 키보드만으로 번개처럼 실행되는 전문 개발자용 커맨드라인 도구입니다.

---

# 6. 제약 사항

## 6.1. AI API 사용 범위
```text
* AI API Key는 환경변수로만 관리하며 코드에 하드코딩하지 않는다.
* 과도한 비용 발생을 방지하기 위해, 1회 실행 시 요청 횟수는 1~2회 이내로 제한하는 것을 권장한다.
* (권장 구현) commit/pr 명령은 각각 AI API 1회 호출로 결과를 생성하고, 로그에 호출 횟수를 출력한다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/ai_client.py` (`AIClient` 클래스, 라인 74, 119)
```python
class AIClient:
    def __init__(self, ...):
        self.call_count = 0  # 인스턴스 생성 시 호출 횟수 0으로 초기화

    def generate_completion(self, ...):
        self.call_count += 1  # AI API를 호출할 때 정확히 1만 올림
```
* 📖 **3개월 차 해설**: 요금이 많이 나오는 것을 막기 위해 AI 호출을 단 1번만 하고, 그 1번 호출한 증거를 터미널 화면 하단에 `AI API 호출 횟수 : 1회`라고 명확히 찍어주는 알뜰 구현입니다.

---

## 6.2. Git 연동 범위
```text
* Git 변경 사항 수집은 git status, git diff 범위로 제한한다.
* 커밋/PR 생성은 초안 텍스트 출력까지를 목표로 한다.
* git push, GitHub PR 생성(API 연동) 등 원격 저장소 자동 반영 기능은 구현하지 않는다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/cli.py` (라인 149~150)
```python
# 결과를 터미널에 출력하는 것으로 파이프라인이 깔끔하게 끝납니다.
# 강제로 git commit이나 git push 명령을 백그라운드에서 날리지 않습니다!
print(formatted)
return 0
```
* 📖 **3개월 차 해설**: AI가 지어낸 문장이 100% 마음에 안 들 수도 있는데 프로그램이 마음대로 원격 저장소에 올려버리면 안 되겠죠? 초안만 딱 보여주고, 실제 적용 여부는 사람이 직접 보고 결정할 수 있도록 안전 경계를 지킨 것입니다.

---

## 6.3. 보안/민감정보

### [세부 제약 3-1 ~ 3-3] 민감정보 프롬프트 제외 및 안전 모드 제공 (A, B 둘 다 구현)
```text
* 보안/민감정보: git diff에 포함될 수 있는 민감정보(API Key, 개인정보 등)는 프롬프트에 포함하지 않도록 주의한다.
* 아래 중 1개 이상을 -safe-mode 같은 옵션으로 제공한다.
   * (A) diff에서 특정 패턴(API Key 형태, 이메일 등)을 마스킹 후 전송
   * (B) diff 일부만 전송(예: 최대 10개 파일, 최대 200줄)
* 생성된 커밋/PR 문구는 최종 정답이 아니며, 사용자가 검토 후 적용한다.
```

### 📍 [코드 구현 위치 및 실제 소스 코드 (라인별 주석)]
* **파일 위치**: `src/security.py` (`mask_sensitive_data` 및 `truncate_diff`, 라인 8~87)
```python
# (A) 정규식 마스킹: 특정 패턴을 찾아내 [REDACTED_...] 로 먹칠합니다.
SENSITIVE_PATTERNS = [
    (re.compile(r"\bsk-[a-zA-Z0-9]{20,}\b"), "[REDACTED_API_KEY]"),       # OpenAI 키 마스킹
    (re.compile(r"\bAKIA[0-9A-Z]{16}\b"), "[REDACTED_AWS_KEY]"),          # AWS 키 마스킹
    (re.compile(r"\b[A-Za-z0-9._%+\-]+@[A-Za-z0-9.\-]+\.[A-Za-z]{2,}\b"), "[REDACTED_EMAIL]"), # 이메일 마스킹
    (re.compile(r'(?i)(password\s*[:=]\s*["\'])([^"\']+)(["\'])'), r"\1[REDACTED_SECRET]\3"), # 비밀번호 마스킹
]

def mask_sensitive_data(text: str) -> str:
    masked = text
    for pattern, replacement in SENSITIVE_PATTERNS:
        masked = pattern.sub(replacement, masked)
    return masked

# (B) 크기 제한: 최대 10개 파일, 최대 200줄까지만 잘라서 보냅니다.
def truncate_diff(diff_text: str, max_files: int = 10, max_lines: int = 200) -> str:
    lines = diff_text.splitlines()
    # 1. 파일이 10개를 넘으면 11번째 파일부터 가차 없이 자름
    file_indices = [idx for idx, l in enumerate(lines) if l.startswith("diff --git ")]
    if len(file_indices) > max_files:
        lines = lines[: file_indices[max_files]]
    # 2. 총 라인 수가 200줄을 넘으면 200줄까지만 자름
    if len(lines) > max_lines:
        lines = lines[:max_lines]
    return "\n".join(lines)
```
* 📖 **3개월 차 해설**:
  > *"우리가 주민등록등본을 남에게 보낼 때 주민번호 뒷자리를 검은 매직으로 까맣게 칠해서(마스킹) 보내잖아요? 내가 실수로 코드에 적어둔 집 비밀번호나 이메일 주소가 AI 회사 서버로 넘어가지 않도록 매직으로 칠해주는 것입니다. 또 문서가 1,000장이나 되면 팩스비가 엄청 나오니까 앞부분 200장만 딱 잘라서 보내는 이중 방패입니다."*
* 💡 **단어 사전**: `pattern.sub(replacement, text)`는 정규식 패턴에 걸린 수상한 문자열을 지정한 가림막 글자(`[REDACTED_SECRET]`)로 싹 바꿔치기(Substitute)해 주는 파이썬 함수입니다.

---

# 7. 전체 파이프라인 종합 요약 다이어그램

```text
[ 1. 사용자 터미널 ]  ===>  python -m src.cli commit -model gpt-4o-mini
       │
       ▼
[ 2. Git 작업공간 ]  ===>  is_git_repository() -> get_changed_files() -> get_git_diff()
       │                     (저장소 확인)         (status 파싱)          (diff 추출)
       ▼
[ 3. 보안 모듈 ]     ===>  apply_safe_mode()
       │                     (API 키/비밀번호 마스킹 & 최대 10파일/200줄 절삭)
       ▼
[ 4. 프롬프트 모듈 ] ===>  build_commit_prompt()
       │                     (System 페르소나/규칙 + User 변경내역 조립)
       ▼
[ 5. AI 연동 모듈 ]  ===>  ai_client.generate_completion()
       │                     (표준 urllib 단 1회 HTTP POST 통신, call_count=1)
       ▼
[ 6. 검증/보정 모듈 ]===>  validate_and_fix_commit()
       │                     (제목 72자 자르기, 빈 본문에 파일/불릿 자동 복구)
       ▼
[ 7. 포매터 모듈 ]   ===>  format_output()
                             (이중 구분선 박스, 메타정보 로깅, 사용자 검토 안내 출력)
```
